# Base das chamadas de API.
# Em produção (container nginx) o proxy reverso resolve /api para o backend,
# então o valor relativo padrão já serve.
VITE_API_BASE_URL=/api

# Usados só por `npm run dev` (fora do Docker).
VITE_DEV_PORT=5173
VITE_DEV_API_PROXY=http://localhost:8000
